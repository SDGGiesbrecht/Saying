ability (clients)
 (
  English: listing of indices of (list) as list of (element) indexed by (index) separated by (boundary)
 )
 {
  ability (clients)
   use of (list) as list of (element) indexed by (index) separated by (boundary)
 }
